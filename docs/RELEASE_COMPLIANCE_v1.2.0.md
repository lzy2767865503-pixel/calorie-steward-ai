# Diet Steward v1.2.0 release compliance / 发布依赖合规记录

**Developer / 开发者：LAI ZEYU (来泽宇)**  
**Evidence time / 证据时间：2026-08-24T16:04:04Z**

The GitHub Release publishes the following machine-generated compliance assets
beside the signed APK. / GitHub Release 在签名 APK 之外同时发布以下机器生成的依赖合规产物：

| Asset / 产物 | Scope / 范围 | SHA-256 |
|---|---|---|
| `diet-steward-v1.2.0-npm-sbom.spdx.json` | SPDX 2.3 production npm SBOM; 484 packages from the locked dependency graph | `a55b7027c9cd1af6b28b6c605b62c914963df4d279447c8df235a2429a836437` |
| `diet-steward-v1.2.0-third-party-licenses.txt` | npm package inventory plus 204 unique installed license/notice texts | `dc52fb2c8fcb9da01bb5121e7172ad6b313889427cba01d71f5c63c19416a4d7` |
| `diet-steward-v1.2.0-android-runtime-dependencies.txt` | Gradle `releaseRuntimeClasspath` dependency inventory; barcode/ML Kit runtime modules absent | `ee194841bf2e575d4ac37953a1aecf17a66ba889e5267686bdebc677e6a5869d` |
| `diet-steward-v1.2.0-android-third-party-licenses.txt` | 160 resolved Android runtime dependencies, 6 embedded notice texts and 0 unresolved license entries | `169eed6a296ae673ab6c212f198b1f0ddbb2d809a3770aad464a349bf0e5fa06` |

The npm assets can be regenerated from the locked mobile dependency tree with
`RELEASE_EVIDENCE_UTC=2026-08-24T16:04:04Z node scripts/generate-release-compliance.mjs`.
This is a regenerability statement for the inventory files, not a claim that the
application build or every regenerated byte is reproducible across environments.
The Android inventory is generated by
`scripts/generate-android-runtime-inventory.mjs` from Gradle's
`:app:dependencies --configuration releaseRuntimeClasspath` task. The generator
rejects local paths and unused barcode/ML Kit runtime modules. The companion
Android license bundle resolves Maven metadata, embedded notices and local Expo
modules covered by the npm SBOM; it reports zero unresolved entries. The final
APK also retains license/notice files emitted by Android packaging.

npm 清单产物可从锁定的移动端依赖树用上述命令重新生成；这是对清单可再生成性的说明，不是对 App 构建或跨环境比特级可复现的声称。Android 清单来自 Gradle 发布运行时依赖报告，
已去除本机路径；最终 APK 也保留了 Android 打包流程输出的许可/声明文件。

These artifacts support technical due diligence but are not legal advice. A
production distributor remains responsible for reviewing every upstream term,
store policy, trademark use, export rule and notice-delivery obligation for its
own distribution channel. / 这些产物用于技术尽调，不构成法律意见。生产分发方仍需根据其渠道审查上游许可、应用商店政策、商标、出口规则和声明交付义务。
